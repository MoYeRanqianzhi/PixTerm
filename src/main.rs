// PixTerm - 终端像素画板程序
// 入口模块：终端初始化/恢复、启动 App

// 引入各子模块
mod app;
mod canvas;
mod color;
mod command;
mod file;
mod history;
mod input;
mod renderer;
mod ui;

fn main() {
    println!("PixTerm - 终端像素画板程序");
}
